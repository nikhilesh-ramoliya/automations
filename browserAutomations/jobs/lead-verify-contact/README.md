# Lead verify contact

Lightweight verification: HTTPS website response, LinkedIn URL shape, optional DNS MX for emails. Provider stub for paid email APIs.

```powershell
npm run jobs:run -- lead-verify-contact --dry-run
```
