# referral-find-people

For each company in `jobs.json`, find **recruiters/talent first**, then peer developers (Full Stack / React / MERN / SWE).

Writes `data/referral/<runId>/people.json`.

```bash
npm run jobs:run -- referral-find-people --dry-run
```
